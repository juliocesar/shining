require File.join(File.dirname(__FILE__), 'spec_helper')

describe "Shining::Slide" do
  it "returns true on #scripted? if the slide has an associated script, false it not"
  it "returns true on #styled? if the slide has an associated stylesheet, false if not"
end